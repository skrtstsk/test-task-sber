pipeline {
    agent any
    stages {
        stage('Download Helm Chart') {
            steps {
                powershell "echo 'Downloading Helm Charts from Github repository...'"
                powershell 'git clone https://github.com/skrtstsk/test-task-sber.git'
            }
        }
    }
}
